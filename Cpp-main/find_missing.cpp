#include <bits/stdc++.h>
using namespace std;

int main()
{

    int arr1[10] = {1, 2, 3, 4, 5, 6, 8, 9, 10};
    int n = sizeof(arr1)/sizeof(arr1[0]);
    // int total = (n) * (n + 1) / 2;

    // for (int i = 0; i < 10; i++)
    // {
    //     total -= arr1[i];
    // }
    // cout << total;
     cout<<n;

    return 0;
}